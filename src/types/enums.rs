#[derive(PartialEq, Clone)]
pub enum ButtonVariant {
    Primary,
    Secondary,
    Tertiary,
    Danger,
    Success,
}

#[derive(PartialEq, Clone)]
pub enum Size {
    Small,
    Medium,
    Large,
}